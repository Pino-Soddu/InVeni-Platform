<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Inveni.App.Pages.InProgrammaPage"
             Title="IN PROGRAMMA">

    <ContentPage.Content>
        <VerticalStackLayout
            Spacing="20"
            Padding="20"
            VerticalOptions="Center"
            HorizontalOptions="Center">

            <Label
                Text="IN PROGRAMMA"
                FontSize="32"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                TextColor="#1A237E" />

            <Label
                Text="Cacce programmate future"
                FontSize="16"
                HorizontalOptions="Center"
                TextColor="#666666" />

            <Label
                Text="🚧 Implementazione in arrivo..."
                FontSize="14"
                HorizontalOptions="Center"
                TextColor="#D84315" />

        </VerticalStackLayout>
    </ContentPage.Content>

</ContentPage>